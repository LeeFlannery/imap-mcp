[project]
name = "imap-mcp"
version = "0.1.0"
description = "Read-only MCP server for searching and reading mail across multiple IMAP accounts"
readme = "README.md"
license = "MIT"
authors = [
    { name = "Lee Flannery" }
]
requires-python = ">=3.12"
keywords = ["mcp", "imap", "email", "model-context-protocol"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Communications :: Email",
]
dependencies = [
    "imap-tools>=1.13.0",
    "mcp[cli]>=1.28.1",
]

[project.urls]
Repository = "https://github.com/LeeFlannery/imap-mcp"
Issues = "https://github.com/LeeFlannery/imap-mcp/issues"

[project.scripts]
imap-mcp = "imap_mcp:main"

[build-system]
requires = ["uv_build>=0.10.9,<0.11.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "ruff>=0.14",
]

[tool.ruff]
target-version = "py312"
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]

[tool.pytest.ini_options]
testpaths = ["tests"]
